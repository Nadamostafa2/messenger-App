class ChatData {
  final String? name;
  final String? msg;
  final String? time;
  final String? image;

  final bool? isActive;
  ChatData(this.name, this.msg, this.time,this.image,{ this.isActive});
}
